const fs = require('fs');

var readContentFromFile = function(fileName,callback){
  fs.readFile(fileName,function(error,content) {
    if(error) {
      return callback(error);
    }
    return callback(null,content);
  });
};


var exportFunctions = {readContentFromFile:readContentFromFile};

module.exports =   exportFunctions;
